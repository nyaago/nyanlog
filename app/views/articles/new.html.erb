<h1>
  <%= @folder.title %> 
  <%= human_action_name(:articles, :new) %>
</h1>

<p id="notice"><%= flash[:notice] %></p>
<%= render 'form' %>

<%= link_to human_action_name(nil, :back), articles_path(:action => :list) %>
